If you wish to rebuild the CD from Windows, see create_bootiso.cmd program!

cygwin.dll is here to run mkisofs.exe in windows.
Both exe files are packed by upx packer and are decompressed automatically.
